export interface signUpFormInterface {
    email: string;
    password: string;
    firstname: string;
    lastName: string;
}
export interface logInFormInterface {
    email: string;
    password: string;
}